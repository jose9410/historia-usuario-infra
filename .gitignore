# Variables de entorno (contienen secretos, NO subir)
.env
.env.local
.env.production

# Docker
docker-compose.override.yml

# Guia de Kubernetes
guia_kubernetes_gitops.md


# Ignore frontend submodule
frontend/

# Ignore backend microservice
backend/

# Terraform — secrets and local state (NEVER commit these)
terraform/terraform.tfvars
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
