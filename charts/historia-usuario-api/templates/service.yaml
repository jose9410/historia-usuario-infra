apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-svc
  labels:
    app: historia-usuario-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: historia-usuario-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
