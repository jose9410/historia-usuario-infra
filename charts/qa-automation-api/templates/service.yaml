apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-svc
  labels:
    app: qa-automation-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: qa-automation-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
